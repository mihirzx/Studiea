// TODO: class overview + per-student scores (Hex/ScoreChart), api/progress.getClassProgress
export default function TeacherDashboard() {
  return (
    <section>
      <h1 className="mb-2 text-2xl font-bold">Class Overview</h1>
      <p className="text-gray-500">TODO: class overview + per-student scores (Hex/ScoreChart), api/progress.getClassProgress</p>
    </section>
  );
}
