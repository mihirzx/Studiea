// TODO: record or upload audio, POST /sessions/upload (multipart 'audio')
export default function SessionUpload() {
  return (
    <section>
      <h1 className="mb-2 text-2xl font-bold">Record or Upload Class Audio</h1>
      <p className="text-gray-500">TODO: record or upload audio, POST /sessions/upload (multipart 'audio')</p>
    </section>
  );
}
